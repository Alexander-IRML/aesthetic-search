Local artwork-filter model artifacts live here.

Do not commit downloaded model weights, trained classifiers, calibration files,
or private datasets. Keep only public-safe documentation and placeholders in Git.

The zero-shot SigLIP 2 checkpoint is managed by the local Hugging Face cache.
Future supervised artifacts belong in versioned subdirectories here and must
include model, embedding-dimension, class-map, and dataset metadata.
